git_url: https://github.com/rahulkj/tap-tools-image
git_branch: main
pivnet_token: ((pivnet_token))
docker_hub_repository: rjain/tap-tools-image

internal_registry:
  host: ((harbor_host))
  username: ((harbor_credentials.username))
  password: ((harbor_credentials.password))

versions:
  tap: 1.12.1
  docker_image: ubuntu:24.10

docker_image:
  maintainer: "rahulkj@gmail.com"
  maintainer_name: "Rahul Jain"
